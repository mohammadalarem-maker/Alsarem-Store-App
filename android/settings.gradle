buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.x.x' // أو أي نسخة لديك
    }
}

// لا تضع أي repositories هنا في الأسفل!
